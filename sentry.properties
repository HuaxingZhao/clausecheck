# Sentry properties — org / project / auth token
# 供 Sentry CLI 上传 source maps 使用（可选，不上传 source map 可不配 token）

defaults.url=https://sentry.io
defaults.org=clausecheck
defaults.project=clausecheck

# 在 Sentry → Settings → Auth Tokens 创建
# auth.token=
